{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "lint": {},
    "test": {},
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**"]
    },
    "dev": {"cache": false, "persistent": true},
    "update": {"cache": false}
  }
}
